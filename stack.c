#include "monty.h"
/**
 * f_stack -  This prints the top
 * @head:Pointer to the head of stack.
 * @counter: line number
 * Return: no return value
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
