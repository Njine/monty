#include "monty.h"
/**
 * f_stack - top printer
 * @head: haed stack pointer
 * @counter: line no
 * Return: no return value
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
